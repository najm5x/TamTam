Expected file: custom_board_01.png
Canvas: 1080x1080
Content: board background/frame art only -- no pieces, names, route
numbers, or gate arrows baked in (those stay programmatic overlays).

Until this file exists, BoardSkin.customTest falls back to the default
board automatically (see board_skin.dart errorBuilder).
